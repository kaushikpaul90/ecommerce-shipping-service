apiVersion: v1
kind: Service
metadata:
  name: shipping-service
spec:
  type: NodePort
  selector:
    app: shipping-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30004
