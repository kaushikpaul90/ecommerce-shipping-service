replicaCount: 1

image:
  repository: "${DOCKER_HUB_USERNAME}/shipping-service"
  tag: latest
  pullPolicy: Always

service:
  type: NodePort
  port: 8080
  nodePort: 30004

containerPort: 8007

hpa:
  enabled: true
  minReplicas: 3
  maxReplicas: 10
  targetCPUUtilizationPercentage: 70
